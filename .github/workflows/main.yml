name: Deploy to MEICaixa BackEnd

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-24.04
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '8.x.x'
    
    - name: Restore dependencies
      run: dotnet restore ./MEI.sln
    
    - name: Build
      run: dotnet build ./MEI.sln -c Release --no-restore
    
    - name: Publish API
      run: dotnet publish ./GestorMEI.API/GestorMEI.API.csproj -c Release --no-build -o ./GestorMEI/GestorMEI.API/publish

    - name: Publish Identity
      run: dotnet publish ./GestorMEI.Identity/GestorMEI.Identity.csproj -c Release --no-build -o ./GestorMEI/GestorMEI.Identity/publish

    - name: Create tarball
      run: tar --no-same-permissions -cvf publish.tar ./GestorMEI
    
    - name: Deploy to MEICaixa API
      uses: appleboy/scp-action@master
      with:
        host: ${{ secrets.VM_HOST }}
        username: ${{ secrets.VM_USERNAME }}
        key: ${{ secrets.VM_SSH_KEY }}
        source: './GestorMEI/GestorMEI.API/publish'
        target: '/var/www/html'
        port: 22

    - name: Deploy to MEICaixa Identity
      uses: appleboy/scp-action@master
      with:
        host: ${{ secrets.VM_HOST }}
        username: ${{ secrets.VM_USERNAME }}
        key: ${{ secrets.VM_SSH_KEY }}
        source: './GestorMEI/GestorMEI.Identity/publish'
        target: '/var/www/html'
        port: 22
    
    - name: Restart service
      uses: appleboy/ssh-action@master
      with:
        host: ${{secrets.VM_HOST}}
        username: ${{secrets.VM_USERNAME}}
        key: ${{ secrets.VM_SSH_KEY }}
        script: |
          sudo -S systemctl restart MEI.API.service
          sudo -S systemctl restart MEI.Identity.service
